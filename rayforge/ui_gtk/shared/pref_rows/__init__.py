"""Preference-row widgets: subclassable spin rows and unit-aware variants.

Each widget class lives in its own ``*_row.py`` module so consumers
import exactly what they need:
``from rayforge.ui_gtk.shared.pref_rows.length_spin_row import LengthSpinRow``.
"""
